{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar C++ ativo",
            "type": "shell",
            "command": "g++",
            "args": [
                "${file}",
                "-g",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compila o arquivo C++ ativo usando g++"
        }
    ]
}